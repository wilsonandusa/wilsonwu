//
stack::emplace
//adds a new element at the top of the stack, above its current top element 
stack::empty
while(!myStack.empty()){
}

//******************
stack::pop

stack::push

stack::size

stack::swap

stack::top
